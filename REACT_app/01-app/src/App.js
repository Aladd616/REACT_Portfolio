import React from "react";
import PortfolioContainer from "./components/PortfolioContainer";
import "bootstrap/dist/css/bootstrap.min.css";

const App = () => <PortfolioContainer />;
//  usestate call api store and array of all projects pre load data in seed or json of projects link the github repo projects.json import into portfolio component and loop through that

export default App;
